% \input{../config}
% \begin{document}

\begin{equation}
  \int_{x \in \Omega} f(x) dx \approx \sum_{i} \gamma(x_i) f(x_i)
\label{eq:quadrature}
\end{equation}
where $\Omega$ is the domain of the quadrature

% \end{document}
